{
  "name": "backlog-tools-portal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy ./public --project-name=backlog-tools-portal"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
